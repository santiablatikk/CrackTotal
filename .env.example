# Configuración del servidor
PORT=3000
STATIC_CACHE_TIME=1d

# Firebase (en desarrollo usa el archivo JSON, en producción usa esta variable)
# FIREBASE_CONFIG={"type":"service_account","project_id":"your-project-id",...} 