import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    // Opciones de producción, si las necesitas
  },
  server: {
    // port: 3000, // Cambia el puerto si lo deseas
    // open: true, // Abre el navegador automáticamente
  }
});
